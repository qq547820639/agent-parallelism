# contract.md — <任务名>

> 派发前必须落盘到项目目录。这是**机制**不是建议：它消除 MAST 中占 44.2% 的规格类失效（最大失效类别）。
> 填完后每个子 agent 的 prompt 首行强制引用本文件。

## 1. 目标与完成判定

- 完成标准（可命令验证，必须可机器判定）：`pytest -x` / `tsc --noEmit` / <自定义门禁>
- 明确**不做**的事：

## 2. 接口契约

| 接口 / 文件 | 签名 | 归属 owner |
|---|---|---|

## 3. 命名表（唯一真源）

| 概念 | 唯一命名 | 禁止使用的别名 |
|---|---|---|

## 4. 写入分片与 owner（物理互斥，禁止越界）

| 分片 | 目录 / 文件范围 | 独占 owner |
|---|---|---|

## 5. 全局不变量（每个子 agent 必须遵守）

- 例：架构约束、字段白名单、双端兼容要求、算子命名规范

## 6. 结构化输出 schema

每个子 agent **必须**按此返回，主 agent 只做拼装、不做语义再加工：

```json
{
  "finding": "",
  "evidence": "<file:line>",
  "confidence": "high|mid|low",
  "blast_radius": "",
  "verification_passed": true
}
```

## 7. 共享背景（冗余率目标 0.41，超过 0.50 与成功率负相关）

- 常驻共享：<指针，不要粘贴全文>
- 按需加载：<文件 → 触发条件>
