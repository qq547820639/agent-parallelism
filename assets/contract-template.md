# contract.md — <任务名>

> 派发前必须落盘到项目目录。这是**机制**不是建议：它消除 MAST 中占 44.2% 的规格类失效（最大失效类别）。
> 填完后每个子 agent 的 prompt 首行强制引用本文件。

## 1. 目标与完成判定

- 完成标准（可命令验证，必须可机器判定）：`pytest -x` / `tsc --noEmit` / <自定义门禁>
- 明确**不做**的事：

## 2. 依赖图与派发顺序

| 子任务 | 依赖（须先完成并合并） | 可并行组 |
|---|---|---|

> 只在上游**完成并合并**后才派发下游。靠 agent 自行判断顺序会漏依赖。

## 3. 接口契约

| 接口 / 文件 | 签名 | 归属 owner |
|---|---|---|

## 4. 命名表（唯一真源）

| 概念 | 唯一命名 | 禁止使用的别名 |
|---|---|---|

## 5. 写入分片与 owner（**软约束，不构成隔离**）

| 分片 | 目录 / 文件范围 | 独占 owner |
|---|---|---|

> ⚠️ 本表只是**软隔离**——CAID 消融实测：软隔离在 PaperBench 上低于单 agent（55.5% < 57.2%）。
> **并发写入的物理隔离必须用 `git worktree`**，本表只用于说明职责划分。

## 6. 全局不变量（每个子 agent 必须遵守）

- 例：架构约束、字段白名单、双端兼容要求、算子命名规范
- **先查再写**：实现任何概念前先确认它是否已被实现，避免 agentic drift（同一概念出现多份冲突实现）

## 7. 结构化输出 schema

每个子 agent **必须**按此返回。与 `subagent-brief-template.md` 的 schema 一致，不得各处不同：

```json
{
  "finding": "<结论，一句话>",
  "evidence": ["<file:line>", "..."],
  "confidence": "high | mid | low",
  "blast_radius": "<影响范围>",
  "verification_passed": true,
  "verification_command": "<实际执行的命令>",
  "blockers": ["<若未完成，说明卡在哪>"]
}
```

主 agent 处理规则：**异题拼装**（各分片负责不同范围时直接组装）、**同题择优**（多个分片对同一问题各出一版时挑最好的，不要融合）。不做语义再加工。

## 8. 共享背景（冗余率目标 0.41，超过 0.50 与成功率负相关）

- 常驻共享：<指针，不要粘贴全文>
- 按需加载：<文件 → 触发条件>
