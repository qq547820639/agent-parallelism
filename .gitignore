# 本地记忆与归档：不进版本库（2026-09-15 用户决定「不提交」）
#
# 这里的记忆包含项目内部笔记与用户偏好，不适合进入本仓库
# （且本仓库可能是公开的）。用 ignore 规则保证它**不可能**被误提交，
# 而不是依赖每次 git add 时记得手动排除。
.codebuddy/

# 同步脚本生成的备份
*.pre-merge

# Python
__pycache__/
*.py[cod]
