---
name: agent-parallelism
description: 当需要决定「要不要并行、并行几个」时加载本 skill——触发情形包括派发多个子 agent / subagent 扇出、并行扫描、并行搜索、并发写入、大范围代码审计、全仓库扫描、多 agent 编排流水线、遇到 429 限流需降并发，或用户要求「加快执行速度」「并行处理」「用多个 agent 一起做」。**即使用户没说"并行"，只要任务结构上是同时推进多件事，就应加载**。用于按 USL 公式 N_max=√((1−σ)/κ) 定并发度（而非拍脑袋），并落实契约先行、git worktree 物理隔离与中心化验证（无验证错误放大 17.2×，有验证 4.4×）。**不要用于**：单 agent 串行任务、简单问答、小改动；**也不用于「并行计算 / 多线程 / 加锁 / 用 xdist 跑测试」这类并发编程与工具级并行**——本 skill 讲的是**多个 agent 的派发**，不是程序内的并发；「加快执行速度」仅指**任务交付速度**，不指程序运行时性能。
when_to_use: 用户可能这样说——「这个仓库 500 多个文件，扫一遍要多久」「能不能几个 agent 一起改」「这个任务能不能拆开同时做」「429 了，降并发」「加快执行速度」「这几件事同时推进」。以上均应触发。（注意：「并行跑测试」若指 pytest-xdist 等**工具级**并行则不触发，本 skill 只管 agent 派发。）
description_zh: "多 Agent 并行派发的并发度决策与协调门禁（USL 定 K、契约先行、worktree 隔离、中心化验证）"
description_en: "Decide parallel agent fan-out width (USL N_max), enforce contract-first, git-worktree isolation and centralized verification"
display_name: "多 Agent 并行调度"
version: 2.2.0
user-invocable: true
agent_created: true
---

# 多 Agent 并行派发

决定「要不要并行、并行几个、怎么防协调失败」。

**不适用**：单 agent 串行任务、简单问答、影响范围明确的局部小改动（可跳过，但须说明跳过原因）。

---

## Gotchas（违反直觉，先读）

1. **任务复杂度分数不能用来决定要不要并行。** 实测 Finance Agent（复杂度 0.407）多 agent **+80.8%**，PlanCraft（0.419）**−70.0%**——复杂度几乎相同，结果相反。只看可分解性三问。
2. **唯一稳健的预测因子是单 agent 基线，不是任务难度。** 但 45% 阈值**跨域失效**（留出整个任务域后 R² = −2.09），只在同一类任务内用。
3. **Amdahl 定律只是 κ=0 的理想上界。** 真实协调开销是二次的（实测回合数 ∝ N^1.724，R²=0.974）。用 Amdahl 估收益会系统性高估。
4. **上下文复制是"贵"，不是"浪费"。** 等预算下多 agent 输给单 agent（数据处理不等式：每次 handoff 都有损），但多 agent 在「单 agent 装不下/被污染」时确实赢。别拿"浪费 token"当反对并行的理由。
5. **编码类不是不能并行，而是分短时程 / 长时程。** 短时程孤立任务（单 issue 修复）：SWE-bench 上多 agent 结构**全部小幅下降** → K=1~2。长时程共享产物（从零建库、复现论文）：CAID **+14.7% / +25.6%** → K=2~4 且须 worktree 隔离。别一刀切。
6. **软隔离吃掉大部分并行收益。** CAID 消融：软隔离（共享 workspace + prompt 分文件）PaperBench **55.5% < 单 agent 57.2%**；Commit0-Lite 56.1% 虽高于单 agent 53.1%，仍显著低于 worktree 的 59.1%。owner 表**只是软隔离**。
7. **并行买的是准确率，不是墙钟时间。** CAID 实测：PaperBench 1803.5s → **2080.4s（更慢）**；Commit0-Lite 692.6s → **1583.2s（慢 2.3×、贵 4.3×）**。诉求是"更快出结果"时，先压单 agent 路径，别加 agent。
8. **通信更多 ≠ 更好。** 混合架构通信开销 +515%，成功率 0.452 反而**低于**单 agent 的 0.466。中心化优于全连通。
9. **测试全绿也可能已经废了。** 多个 agent 各自独立实现同一概念（不同类名 / 接口 / 假设），代码能编译、测试能过，但代码库里躺着 3 份互相冲突的实现——这叫 **agentic drift**，是并行写入最阴险的失败模式。合并步骤**除了跑测试，必须专门查重复实现**。
10. **子 agent 自报"完成"不可信。** MAST 发现验证器普遍只做表面检查（"能编译吗"）——一个生成的国际象棋程序通过表面检查却有运行时 bug。验收必须命令化、可机器判定。

---

## 先分清：工具并行 vs agent 并行

**很多"并行"根本不需要派 agent。**

| | 工具级并行（同 agent 批量工具调用）| agent 级并行（派子 agent）|
|---|---|---|
| 上下文 | 共享，无复制 | 每个子 agent 独立窗口 |
| 固定开销 | ≈0 | **约 20k tokens/个**（业界报告值，量级参考；哪怕只读一个文件就返回）|
| 适用 | 批量搜索 / 读取 / 检查 | 需隔离、压缩、独立推理链、工具限制或标准化 |

**判定顺序**：先问"能不能在主线程用并行工具调用解决"——能就不派 agent。
**不派判据**：写 context 的成本 > 直接做掉的成本。

> 分片依据：按**概念 / 功能**切，不要按目录切。目录分片会在跨目录引用处失效（"查 auth 系统"优于"查 `src/auth/`"）。

---

## 决策流程

### Step 0 · 测单 agent 基线，对照 45% 阈值

*为什么*：基线越高，留给协作弥补的错误越少，而通信开销不会自动消失。

| 单 agent 基线 | 结论 |
|---|---|
| > ~45% | **默认单 agent**，加 agent 收益通常归零或转负 |
| < ~45% | 进入 Step 1 |

> ⚠️ **不要把"先让单 agent 跑完一遍"当必经步骤。** CAID 实测「单 agent 跑完 → 再切多 agent」：runtime 与 cost 近乎相加而性能仅略升（PaperBench/Claude：63.3%@2080s → 66.8%@**3884s**；Commit0/MiniMax：得分**完全相同** 57.0%，runtime 1909s → 2661s）。**测基线是为了判断，不是为了试错**——测完直接选架构。

### Step 1 · 可分解性三问（逐条判定，任一命中否决条件即 K=1）

| 问题 | 否决条件 |
|---|---|
| 子任务能否**同时推进**？ | 否 → K=1 |
| 各分支是否依赖**同一份持续变化的状态**？ | 是 → K=1 或 K=2（且必须强契约） |
| 中间结果能否被**独立验证**？ | 否 → K=1 |

### Step 2 · 定 K：跑脚本，不要拍脑袋

```
C(N) = N / (1 + σ(N−1) + κN(N−1))
N_max = √((1 − σ) / κ)
```

- `σ`：串行/排队占比（契约层 + 收敛层）
- `κ`：串扰/一致性开销，**二次增长**（上下文碎片化、契约漂移、合并对账）
- `κ = 0` 时退化为 Amdahl 定律

**speedup 的操作定义**：串行耗时 ÷（并行耗时 **+ 合并耗时 + 返工耗时**）。只记并行耗时会漏掉协调成本，拟合出的 κ 会系统性偏小、K 偏大。

**有实测数据** → 直接跑（无网络、只读；有 scipy 时走 NLS 并给 N_max 置信区间，无 scipy 自动回落零依赖网格搜索）：

```bash
python3 scripts/fit_kappa.py --csv assets/parallel-log.csv
```

输出 σ、κ、N_max、R²、建议 K，并诊断**曲线形状**：仍在上升 → σ 侧（串行）或尚有余量；**已过峰值 → κ 侧，对策是减少共享（分区 / 每片独立状态 / 批量更新），不是调优并行代码**。置信区间需 **≥5 个不同 N**——2 参数拟合 4 个点自由度不足，区间只会是虚假精度。

**无实测数据** → 用下表，并在任务后补测：

| 场景 | σ | κ | N_max | 建议 K |
|---|---|---|---|---|
| 只读 / 独立分片 / 有验收 | 0.10 | 0.01 | ≈9.5 | **8~9** |
| 写入 / 共享状态 / 跨模块 | 0.15 | 0.05 | ≈4.1 | **2~4** |
| 编码主链路 | 0.15 | 0.05 | ≈4.1 | **1~2** |
| 不确定 | — | — | — | **1** |

**实测锚点**（CAID Figure 3，共享产物长时程任务）：Commit0-Lite 上 2 → 4 个 engineer 继续提升，**增至 8 反而下降**；PaperBench 上超过 2 个后增益极小而 runtime/cost 持续上升。→ 这类任务 **K=2~4 是经验甜点，不要到 8**。

> κ 默认值是**猜测**。跑过 3~5 个不同 K 后用脚本回归替换——这是本 skill 唯一能让数字变准的机制。

### Step 3 · 中心化验证（不可省略）

| 结构 | 轨迹级错误放大 |
|---|---|
| 独立（无中心验证） | **17.2×** |
| 中心化（orchestrator 汇总前核对） | **4.4×** |

子 agent 输出必须经主 agent 或显式 verifier 核对后才合并，**禁止直接拼接**。
MAST 实测：增加高层目标验证步骤 **+15.6%** 成功率，是所有干预中收益最大的一项。
CAID 补充（Figure 5）：manager 复核 **60.2%** > engineer 自验证 **55.1%** > 效率优先 **54.0%**，代价是 runtime 2243.9s → 3689.1s（+64%）。**要质量用中心复核，要速度可退到自验证——但不能放弃验证。**

**择优，不要融合。** 多个子 agent 对**同一问题**各出一版时，让 orchestrator 挑最好的那份，而不是把几份揉成一份：实测 judge-based selection 比 synthesis 高 **Δ_WR = +0.631**，42 个任务中 synthesis 在 **0 个**任务上被判定优于基线（arXiv:2603.20324，单作者预印本，N=210）。**规则：同题择优、异题拼装**——分片各管不同范围时，拼装才是对的。

⚠️ **不要用「多数一致」当验收。** 模型共享预训练数据，错误是相关的，Condorcet 陪审团定理的独立性假设不成立——几个 agent 一起自信地答错，共识会把错误**放大**而不是纠正。

**模型配置**：orchestrator 用强模型，**worker 池内部保持同质**。Anthropic 生产架构即 Opus 4 主 agent + Sonnet 4 子 agent（+90.2%）；CAID 的收益在弱模型上最大（MiniMax +25.6 vs Claude +6.1）。但 worker 池内混用强弱会拖累——Nature MI 探索性实验：中心化异构团队比强模型同构团队低 **12.6 个百分点**。

### Step 4 · briefing 冗余率 ≈0.41

注入每个子 agent 的共享背景占比，目标 **0.41**（中心化结构中位数）。**> 0.50 后与成功率负相关**。
背景材料**外置为文件按需 Read**，不要在 prompt 里重复粘贴。

---

## 分片与隔离：git worktree（不可降级为软隔离）

**分解质量决定质量上限**（分解器之于多 agent，犹如查询计划器之于数据库——计划烂了，agent 再强也白搭）：

- **先隔离枢纽**：被大量文件依赖的共享件（基类 / 公共工具 / 中央类型）单独成片或由主线程统一改。否则它会被拖进每一个分片，把分区彻底破坏
- **粒度**：每个 agent **5~6 个子任务**为宜。粒度悬殊时（一个 100ms、一个 60s），关键路径被最粗的那个吃掉，并行收益归零
- **近全耦合就不分**：几乎所有文件互相依赖时，分区只会返回单一社区 → 退化成串行，分区本身反而是净开销
- **重规划上限 2~3 次**：超过就降级为单 agent，避免「分解 → 失败 → 再分解」死循环

并发写入与长时程任务，隔离必须是**物理**的：

```bash
git worktree add .worktrees/<分片名> -b <branch>   # 每个分片一个独立工作目录
# 子 agent 在自己的 worktree 内跑测试 → 通过才 commit
git merge <branch>                                  # 冲突由该分片的 agent 自行解决
```

三个原语（来自 CAID，CMU / OpenHands），缺一不可：**依赖图 DAG**（仅在上游合并后派发下游）、**worktree 物理隔离**（软隔离实测劣于单 agent，见 Gotcha 6）、**测试门控合并**（测试通过才 merge，main 始终可用）。

补充：收益强弱不对称——CAID 在 PaperBench 上 MiniMax 2.5 **+25.6**、Claude 4.5 Sonnet 仅 **+6.1**，独立佐证「能力饱和」：**模型越强，并行收益越小**。

---

## 限流是另一回事（K 的实际上限）

**协调成本 ≠ 限流。** 协调成本是内部分支干扰——质量下降、返工增多，但**不报错**；限流是供应商容量——显式 **429**。两者都把 K 往下压，但机理不同：**用降 K 治限流，等于白白牺牲吞吐。**

**有效 K = min(K_协调最优, K_限流上限)。**

限流上限**没有公式，只能实测探测**：用 AIMD（成功 +1 ／ 429 ×0.75 ／ 级联抑制 ／ 天花板稳定）。**固定并发是错的**——让系统自己找到上限。参数见 `references/关键数字速查.md` §10。

---

## 派发前检查清单

逐项勾选 `assets/dispatch-checklist.md`，**任一项未过 → 降到 K=1**。

> *为什么契约是硬要求*：MAST 统计中**规格类失效占 44.2%**，是所有失效类别里最大的一类；仅修正角色规格即带来 **+9.4%** 成功率。

---

## 资源（按需加载，不要默认全读）

| 文件 | 何时加载 |
|---|---|
| `assets/dispatch-checklist.md` | 派发前逐项勾选（`contract.md` 同理）|
| `assets/contract-template.md` | 要落盘 `contract.md` 时 |
| `assets/subagent-brief-template.md` | 确定要派子 agent 后，写给每个子 agent 的 prompt |
| `assets/parallel-log.csv` | 任务结束后记录观测，或拟合 κ 前 |
| `evals/` | 改动 `description` 后回归触发精度；改动流程后回归输出质量 |
| `scripts/fit_kappa.py` | **直接运行**，不要读入上下文 |
| `scripts/selfcheck.py` | **改动本 skill 任一文件后跑一次**（74 项结构不变量断言，exit 0 才算过）|
| `references/关键数字速查.md` | 需要核对任何数字的口径、来源或修正记录时（**优先读这个**）|
| `references/研究依据_多Agent并行.md` | 仅在：需要引用原始出处 / 需要 MAST 完整 14 种失效模式 / 需要信源可信度评估 |

**默认不要读 references。** 它对决策不是必需的，读了只是烧上下文。

---

## 边界与漂移

**确有大幅收益的三类任务**：广度优先研究（Anthropic **+90.2%**，内部 eval，不可外推为行业基准）、长时程共享产物（CAID **+14.7% / +25.6%**，须 worktree 隔离）、可切分的多源分析（**+80.8%**）。

**但数字会漂移**：以上均来自 2026 年模型能力与实验条件。Nature MI 标题即 *Capable language models can **outgrow** the benefits of collaboration*——模型越强，协作收益越消退。**每 6 个月或主力模型换代后重测基线并重新拟合 κ。** 口径见 `references/关键数字速查.md`。
